<?php

class StudentController
{

    public function show($id)
    {
        var_dump($id);
        // $result = $db->query('SELECT * FROM users WHERE id =' . );
        // $results->setFetchMode(PDO::FETCH_ASSOC);
        // while($row = $results->fetch()) var_dump($row);
    }
}